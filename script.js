let config = {
    type: Phaser.WEBGL,
    width: 800,
    height: 800,
    backgroundColor: 0x214026,
    physics: {
        default: 'arcade'
    },
    scene: []
}

let game = new Phaser.Game(config);